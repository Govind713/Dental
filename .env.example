# Copy this file to .env and fill values
PORT=3000
# SMTP (example: Gmail SMTP or your mail provider)
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_USER=you@example.com
SMTP_PASS=yourpassword
# From email (optional)
FROM_EMAIL=noreply@yourdomain.com
# Where contact/appointment emails are sent
TO_EMAIL=clinic@yourdomain.com
